<% if @question.valid? %>
  $("#question_<%= @question.id %>").replaceWith('<%= render 'question', question: @question %>')
<% else %>
  $("#errors_<%= @question.id %>").html('<%= @question.errors.full_messages.join(',') %>')
<% end %>
