<% if @answer.valid? %>
  $("#answer_<%= @answer.id %>")
    .replaceWith('<%= j(render 'answer', answer: @answer, user: current_user) %>')
<% else %>
  $("#errors_<%= @answer.id %>").html('<%= @answer.errors.full_messages.join(',') %>')
<% end %>
